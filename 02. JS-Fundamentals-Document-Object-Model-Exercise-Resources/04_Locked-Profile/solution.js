function solve() {
   let users = document.querySelectorAll('div.profile');
   let userOne = users[0];
   console.log(userOne.childNodes)
   console.log(users);

   let buttons = document.querySelectorAll('button')

   console.log(buttons);
   
} 